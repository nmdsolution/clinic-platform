# version: "3.7"

services:
  gateway:
    build:
      context: ./gateway

  frontend:
    build:
      context: .
      dockerfile: frontend/overlay.Dockerfile

  backend:
    build:
      context: .
